# The docker run steps in the README use a demo-only POSTGRES_PASSWORD=secret
# so the volume lesson stays a short copy-paste (see the callout there).
# Compose still keeps the password out of YAML — copy this file to .env.
POSTGRES_PASSWORD=change-me

NODE_ENV=production
PORT=3000
APP_VERSION=replace-with-git-sha
# Required by compose.prod.yaml — pin the image CI pushed (not "latest").
# IMAGE_REF is ":sha-…" (tag) or "@sha256:…" (digest).
# REGISTRY_OWNER=YOUR_GITHUB_USER
# IMAGE_REF=:sha-abc1234

# Copy to .env for the optional Compose form. Never commit .env.
# The docker run steps in the README use the same password inline so the
# volume lesson stays a short copy-paste; Compose still keeps it out of YAML.
POSTGRES_PASSWORD=change-me

# syntax=docker/dockerfile:1
# Secret is mounted only for this RUN. It must not land in a layer.
FROM alpine:3.22
RUN --mount=type=secret,id=demo \
    test -s /run/secrets/demo \
    && echo "secret was readable during this RUN only"
USER nobody
CMD ["echo", "runtime image has no build secret"]

# Single-stage (larger / more than needed) — for comparison only
FROM node:22-alpine
WORKDIR /app
COPY package.json ./
RUN npm install
COPY build.js ./
RUN npm run build
EXPOSE 3000
CMD ["node", "dist/server.js"]

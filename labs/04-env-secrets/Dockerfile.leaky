# Demonstration only — do not copy this pattern.
# A secret baked into an ENV instruction is visible in image history.
FROM alpine:3.22
ENV DB_PASSWORD=super-secret-do-not-copy
CMD ["printenv", "DB_PASSWORD"]
